# Core execution defaults. Keep DEFAULT_NETWORK=localhost for the canned Hardhat
# node, or point it to a public network once you wire contracts back in.
DEFAULT_NETWORK=localhost
INFURA_API_KEY=
ETHEREUM_PRIVATE_KEY=ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
REPORT_GAS=false
COINMARKETCAP_API_KEY=
ETHERSCAN_API_KEY=
GELATO_RELAYER_API_KEY=

# Frontend configuration defaults to “no on-chain contracts” by leaving the
# contract addresses unset (all zeros). Provide real addresses to re-enable
# on-chain mode.
NEXT_PUBLIC_DEFAULT_NETWORK=localhost
NEXT_PUBLIC_INFURA_API_KEY=
NEXT_PUBLIC_FEEDBACK_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
NEXT_PUBLIC_SEMAPHORE_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
NEXT_PUBLIC_GROUP_ID=0
NEXT_PUBLIC_OPENZEPPELIN_AUTOTASK_WEBHOOK=
NEXT_PUBLIC_GELATO_RELAYER_ENDPOINT=
NEXT_PUBLIC_GELATO_RELAYER_CHAIN_ID=
# Hardhat / proving artifacts (for tests)
# If you place files under apps/web-app/public/artifacts, you can omit these.
# Otherwise, set absolute paths for the test helper to find them.
# SNARK_WASM=/absolute/path/to/semaphore.wasm
# SNARK_ZKEY=/absolute/path/to/semaphore.zkey
# SNARK_DEPTH=20

# Local server URL for tests that relay via API
# SERVER_URL=http://localhost:4000
